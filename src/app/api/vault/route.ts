import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'vault API coming in Phase 4' })
}

export async function POST() {
  return NextResponse.json({ message: 'vault API coming in Phase 4' })
}