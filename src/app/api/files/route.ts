import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'file API coming in Phase 6' })
}

export async function POST() {
  return NextResponse.json({ message: 'file API coming in Phase 6' })
}